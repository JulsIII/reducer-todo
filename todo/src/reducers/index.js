const reducer = (state, action) => {

    switch(action.type){
        case('SET_TODO_TITLE'):
        case('SET_EDITING'):
        case('SET_NEW_TEXT_TITLE'):
        default:
            return(state);
    }
}